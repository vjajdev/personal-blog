---
description: force push to remote without user interact
---

// turbo-all

1. Add all files to staging
2. Commit the files
3. Push to remote

Run the following command:
`git add . && git commit -m "Auto push" ; git push origin HEAD`
